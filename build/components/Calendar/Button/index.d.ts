import React from 'react';
import { IButtonProps } from './types';
declare const Button: React.MemoExoticComponent<
    ({ text, onButtonClick }: IButtonProps) => React.JSX.Element
>;
export default Button;
